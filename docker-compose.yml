services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--root-path", "/api"]
    env_file:
      - .env
    volumes:
      - ./data:/app/app/data
    restart: unless-stopped
